package sparklicorn.strings;

public class Palindrome {

	/**
	 * Determines whether <code>str</code> is a palindrome.
	 * A palindrome is spelled the same way forwards and backwards.
	 * Eg. "racecar", "a", "abcddcba" are palindromes.
	 * @param str The String to check.
	 * @return True if <code>str</code> is a palindrome; otherwise false.
	 * @throws NullPointerException if <code>str</code> is null.
	 * @throws IllegalArgumentException if <code>str</code> is empty.
	 */
	public static boolean isPalindrome(String str) {
		//TODO
		
		return false;
	}
}
