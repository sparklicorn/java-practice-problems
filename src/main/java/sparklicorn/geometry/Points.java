package sparklicorn.geometry;

public class Points {
	
	public static final double EPSILON = 1e-10;
	
	/**
	 * Gets the distance between the points.
	 * @param p1 The first point.
	 * @param p2 The second point.
	 * @return The distance between the points.
	 * @throws NullPointerException if either point is null.
	 */
	public static double distance(Point p1, Point p2) {
		//TODO
		
		return 0.0;
	}
	
	/**
	 * Checks whether <code>point</code> lies within <code>circle</code>.
	 * @param point A Point
	 * @param circle A Circle
	 * @return True if the point is within the bounds of the circle
	 * or within <code>EPSILON</code> of its edge; otherwise false.
	 * @throws NullPointerException if <code>point</code> or <code>circle</code>
	 * are null.
	 */
	public static boolean isPointInCircle(Point point, Circle circle) {
		//TODO
		
		return false;
	}
	
	
}
